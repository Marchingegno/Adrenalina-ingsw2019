package it.polimi.se2019.network.message;

public class WeaponMessage extends Message {



	public WeaponMessage(MessageType messageType, MessageSubtype messageSubtype) {
		super(messageType, messageSubtype);
	}
}
